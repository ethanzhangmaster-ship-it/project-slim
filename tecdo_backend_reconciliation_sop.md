# TecDo 后台核对 SOP

## 目标

确认下面这件事到底卡在哪一层：

- TecDo OpenAPI 已授权
- 账号在账号接口里可见
- 但报表接口返回 `code=0` 且 `rows=0`

要核实的是：

1. 这些账号在 TecDo 后台是否真实有投放数据
2. 后台看到的数据，是否就是 OpenAPI 报表接口同一口径
3. 是否存在账号已授权但未开通报表接口数据的情况

## 本地先看这三份文件

1. `output/active/tecdo_probe_20260603.md`
2. `output/active/tecdo_account_reconciliation_20260603.md`
3. `output/active/creative_source_readiness_20260603.md`

## 当前待核的账号

- `1628583695016910` | `GAMEGZZ_Tec_Do_04_260312_IOS_3`
- `1379499207181514` | `GAMEGZZ_Tec_Do_04_260312_IOS_2`
- `907214188838822` | `GAMEGZZ_Tec_Do_02_260227_IOS_2`
- `25912976875001336` | `GAMEGZZ_Tec_Do_07_260205_AND_1`
- `1455525822955003` | `GAMEGZZ_Tec_Do_04_260115_AND_1`
- `1423660739468966` | `GAMEGZZ_Tec_Do_04_260115_IOS_1`

## 后台核对步骤

1. 进入 TecDo 后台的账号列表页。
2. 搜索 `mediaAccountId`，确认账号名称与本地清单一致。
3. 进入对应账号的报表页。
4. 把日期切到：
   - `2026-05-28` 到 `2026-06-03`
5. 再切到更长窗口：
   - 最近 30 天
   - 最近 180 天
6. 记录下面三件事：
   - 后台页面是否有花费
   - 后台页面是否有点击 / 展现 / 安装
   - 后台页面的数据层级是账号级、Campaign级、Adset级，还是广告级
7. 如果后台页面有数据，但本地 OpenAPI 仍是 `rows=0`，继续记录：
   - 后台页面路径
   - 页面名称
   - 当前筛选条件
   - 是否有“API报表开通”或“数据权限”相关开关
8. 如果后台页面也没有数据，记录：
   - 该账号是否只是已开户未消耗
   - 是否为历史停用账号
   - 是否看错平台或主体

## 判断标准

### 结果 A

后台有数据，OpenAPI 也应该有数据。

说明：

- 更可能是 API 口径、过滤条件、或数据空间没对上。

下一步：

- 找 TecDo 技术支持核对 `report/query` 和后台报表是否同源。

### 结果 B

后台没有数据。

说明：

- 账号真实存在，但当前不是可用素材数据源。

下一步：

- 不再把 TecDo 当作当前素材链路来源。

### 结果 C

后台有数据，但不是这套 OpenAPI 报表口径。

说明：

- 当前接口选错了，或者还缺另一个报表接口。

下一步：

- 继续让系统改接正确接口，而不是继续盯现有 `report/query`。

## 产出要求

核完之后，把结果填到：

- `tecdo_backend_reconciliation_log.md`

至少补这几列：

- 账号
- 后台是否有数据
- 后台看的是哪个页面
- 最近 7 天是否有花费
- 最近 180 天是否有花费
- 与 OpenAPI 是否一致
- 结论
- 下一步找谁
