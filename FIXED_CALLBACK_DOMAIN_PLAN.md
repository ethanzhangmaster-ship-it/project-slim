# 固定回调域名方案

## 目标

把当前临时 `trycloudflare` 地址改成固定回调地址，避免：

1. 每次重启后地址变化
2. 飞书后台重复改回调地址
3. 机器人临时失效

## 当前状态

当前链路已经闭环，但公网入口仍是临时地址。

当前临时方案：

- 本地回调服务
- cloudflared quick tunnel
- 飞书后台手动填最新地址

## 推荐方案

### 方案 A：Cloudflare 固定 Tunnel + 自有域名

这是最推荐的方案。

你需要提供：

1. 一个 Cloudflare 账号
2. 一个已经接入 Cloudflare 的域名
3. 该域名下一个子域名

例如：

- `bot.yourdomain.com`

落地后效果：

- 飞书后台回调地址固定不变
- 机器人重启后不需要再改飞书后台
- 当前脚本只需要改成连接 named tunnel

### 方案 B：云服务器固定反向代理

你需要提供：

1. 一台公网云服务器
2. 一个域名或子域名

落地方式：

- 服务器上跑固定回调入口
- 本地或服务器内再转发到当前 Python 服务

适合：

- 已有服务器运维环境
- 不想依赖 Cloudflare quick tunnel

### 方案 C：继续使用临时地址

这是当前方案，不需要新增资源，但维护成本最高。

## 我下一步能直接做的内容

只要你给到固定域名方案所需资源，我可以继续做：

1. 改启动脚本
2. 改健康检查脚本
3. 改回调配置展示文件
4. 改总门禁脚本
5. 把文档统一更新为固定地址版本

## 如果你选 Cloudflare 固定 Tunnel

你下一步只需要给我下面任意一种：

1. Cloudflare Zero Trust / Tunnel 登录后的可用环境
2. 域名和子域名
3. 你希望绑定的固定地址

例如：

- `bot.xxx.com`

## 如果你选云服务器方案

你下一步只需要给我：

1. 服务器地址
2. 登录方式
3. 目标域名

## 当前结论

当前系统已经可用。

现在唯一还值得继续做、而且能明显降低维护成本的，就是：

固定飞书回调公网地址。

## 配套文件

- 接入信息模板：`FIXED_CALLBACK_DOMAIN_INTAKE.txt`
- 落地检查清单：`FIXED_CALLBACK_DOMAIN_CHECKLIST.md`
