# 群里提需求机器人最小版本方案

## 目标

在飞书群里支持两类新能力：

1. 自由问答
2. 提需求并生成待办

先不直接开放“改配置”和“直接改系统”。

第一阶段只做：

- 能回答现有系统和本周周报相关问题
- 能识别需求类消息
- 能把需求整理成结构化待办

## 为什么先做最小版本

当前周报机器人已经能稳定做：

- 固定指令回复
- 周报卡片发送
- 发前门禁

但它还不适合直接接管“改需求”或“改配置”，原因有两个：

1. 风险高
2. 群里自然语言往往不够精确

所以更合理的最小版本是：

- 问题类：直接答
- 需求类：先归纳、确认、生成待办

## 最小版本能力边界

### A. 直接回答的问题

只回答当前系统能从本地产物中可靠拿到的问题：

- 本周公司花费是多少
- 本周 ROI 是多少
- 哪个项目风险最大
- 哪个群已经接通
- 当前回调地址是什么
- 机器人为什么没回复
- 现在能发周报了吗
- 哪些数据可信，哪些不可信

### B. 可以识别但不直接执行的问题

这类消息先转成结构化待办：

- 把老板版文案改短一点
- 下周多加一个项目分析
- 以后别发某个群
- Google 素材口径改一下
- 增加一个新数据源

输出形式：

- 需求摘要
- 影响范围
- 建议动作
- 是否需要人工确认

### C. 不允许直接执行的动作

最小版本先禁止：

- 改 `.env`
- 改 webhook
- 改白名单
- 改发送目标群
- 改定时任务
- 直接发群
- 直接删配置

这些都只能：

- 生成待办
- 或提示“需要在 Codex 里继续处理”

## 群里消息分流逻辑

收到 `@机器人` 后，先做三层判断：

### 第一层：固定指令优先

如果命中下面这些，仍走当前稳定链路：

- 帮助
- 简版
- 详细版
- 回收版
- 老板版
- 摘要

### 第二层：问答类

如果像下面这些句式：

- 本周 ROI 是多少
- P04 为什么不建议加量
- 当前哪个群接通了
- 现在能发吗

则进入问答模式。

回答来源只允许用：

- `output/active/`
- 当前状态文件
- 当前周报产物
- 当前回调状态产物

### 第三层：需求类

如果像下面这些句式：

- 帮我改…
- 以后增加…
- 下周别发…
- 增加一个…
- 调整一下…

则进入需求识别模式。

返回：

- 我理解的需求
- 影响范围
- 建议下一步
- 是否已生成待办

## 最小版本回答来源

为了保证可控，第一阶段不接外部大模型自由推理。

优先用本地规则 + 本地产物检索：

- `market_ops_status_latest.*`
- `weekly_release_gate_latest.*`
- `pre_send_summary_*.json`
- `card_preview_*.json`
- `weekly_metrics_consistency_latest.*`
- `feishu_callback_live.*`

回答方式：

1. 先做关键词分类
2. 再读取对应文件
3. 最后拼出简短回答

## 最小版本需求待办格式

建议新增一个本地待办文件：

`output/active/group_requirements_queue.json`

每条待办字段：

- id
- created_at
- chat_id
- user_text
- intent_type
- request_summary
- suggested_scope
- risk_level
- requires_manual_confirmation
- status

示例：

```json
{
  "id": "req_20260610_001",
  "created_at": "2026-06-10 16:50:00",
  "chat_id": "oc_xxx",
  "user_text": "把老板版文案再压缩一点",
  "intent_type": "change_request",
  "request_summary": "压缩老板版周报文案",
  "suggested_scope": ["boss_report_copy", "executive_summary"],
  "risk_level": "low",
  "requires_manual_confirmation": true,
  "status": "new"
}
```

## 群里回复模板

### 问答类模板

```text
当前结论：
本周公司总收入 ROI 为 1.47。

补充说明：
这来自本周发前摘要和市场周报口径。
如需看完整内容，请发送：@机器人 详细版
```

### 需求类模板

```text
我理解你的需求是：
压缩老板版周报文案。

影响范围：
老板版周报 / 发前摘要。

当前处理方式：
已记录为待办，暂不直接改线上配置。
如需我继续执行，需要在 Codex 会话里继续处理。
```

## 技术实现建议

### 第一阶段

新增一个轻量路由层：

- `qa`
- `change_request`
- `unsupported`

建议新增文件：

- `src/market_ops/group_qa_router.py`
- `src/market_ops/group_requirements_queue.py`

在 `event_server.py` 里接入：

1. 固定指令优先
2. 再尝试问答
3. 再尝试需求识别
4. 不识别则返回帮助

### 第二阶段

如果第一阶段稳定，再考虑：

- 接 OpenAI 做更自然的问答归纳
- 允许“低风险配置”直接执行
- 支持管理员确认后自动改配置

## 风险控制

最小版本必须坚持：

1. 不自动改配置
2. 不自动发新群
3. 不自动改白名单
4. 不自动改定时任务
5. 不自动改 webhook

否则会把当前稳定链路打乱。

## 推荐推进顺序

1. 先做问答模式
2. 再做需求识别 + 待办落盘
3. 最后再考虑审批后执行

## 当前结论

最小版本不做“万能机器人”。

先做成一个：

- 能回答当前系统问题
- 能接收需求并整理待办
- 不直接动关键配置

这才是当前最稳、最适合上线到群里的版本。
